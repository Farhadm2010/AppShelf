# AppShelf
macOS app launcher. Organize apps into pages, folders, drag to reorder, search all pages, hide/restore apps.  **Install** 1. Download AppShelf.zip 2. Unzip → drag to Applications 3. First launch: right-click → Open → Open 4. Auto-start: System Settings → Login Items → add AppShelf  Requires macOS 13+
